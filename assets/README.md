# This folder is for Stage 0 manual branding mode.
# Drop your logo file here as 'logo.svg' or 'logo.png' to use it in the report.
# Logo files are gitignored by default so they won't leak into public forks.
